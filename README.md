# Animal Shelter

## Java JumpStart Tutorial Repo

### Git Commands

* `git init`
* `git add`
* `git commit -m "message"`
* `git diff`
* `git pull`
* `git push`
* `git branch`
* `git checkout`
* `git merge`
* `git status`
* `git log`
* `git remote add origin`
* `git push -u origin main`

